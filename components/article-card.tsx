import Link from 'next/link';import {ArrowUpRight} from 'lucide-react';import {Article} from '@/lib/content';
export function ArticleCard({a}:{a:Article}){return <article className="card overflow-hidden group"><Link href={`/blog/${a.slug}`}><img src={a.image} alt={a.title} loading="lazy" className="w-full aspect-[16/9] object-cover transition duration-500 group-hover:scale-[1.025]"/></Link><div className="p-5"><div className="text-xs font-bold uppercase tracking-wider" style={{color:'var(--primary)'}}>{a.category}</div><Link href={`/blog/${a.slug}`}><h3 className="text-xl font-bold mt-2 leading-snug group-hover:underline">{a.title}</h3></Link><p className="muted text-sm mt-2 leading-6">{a.excerpt}</p><div className="flex items-center justify-between mt-5 text-xs muted"><span>{a.author} · {a.date}</span><span>{a.readingTime} min read <ArrowUpRight className="inline" size={13}/></span></div></div></article>}
