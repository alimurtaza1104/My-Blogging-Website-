import Link from 'next/link';import {categories} from '@/lib/site';export default function Categories(){return <main className="container py-16"><h1 className="text-5xl font-black">Categories</h1><p className="muted text-lg mt-4">Explore the subjects covered by NextByte Journal.</p><div className="grid md:grid-cols-2 gap-5 mt-10">{categories.map(c=><Link key={c.slug} href={`/category/${c.slug}`} className="card p-7 hover:-translate-y-1 transition"><div className="text-sm font-bold" style={{color:'var(--primary)'}}>CATEGORY</div><h2 className="text-2xl font-bold mt-2">{c.name}</h2><p className="muted mt-2 leading-7">{c.description}</p></Link>)}</div></main>}
