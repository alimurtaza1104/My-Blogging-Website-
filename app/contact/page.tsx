'use client';import {useState} from 'react';export default function Contact(){const[sent,setSent]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});if(r.ok){setSent(true);e.currentTarget.reset()}}return <main className="container py-16 max-w-3xl"><div className="text-sm font-bold uppercase tracking-wider" style={{color:'var(--primary)'}}>Contact</div><h1 className="text-5xl font-black mt-2">Get in touch.</h1><p className="muted text-lg mt-4">For editorial questions, corrections, partnerships or general inquiries.</p>{sent&&<div className="card p-4 mt-7" role="status">Thanks. Your message has been received.</div>}<form onSubmit={submit} className="grid gap-4 mt-8"><input name="name" required placeholder="Name" className="border rounded-xl px-4 py-3 bg-transparent" style={{borderColor:'var(--line)'}}/><input name="email" required type="email" placeholder="Email" className="border rounded-xl px-4 py-3 bg-transparent" style={{borderColor:'var(--line)'}}/><input name="subject" required placeholder="Subject" className="border rounded-xl px-4 py-3 bg-transparent" style={{borderColor:'var(--line)'}}/><textarea name="message" required rows={7} placeholder="Message" className="border rounded-xl px-4 py-3 bg-transparent" style={{borderColor:'var(--line)'}}/><input name="website" tabIndex={-1} autoComplete="off" className="hidden"/><button className="justify-self-start px-6 py-3 rounded-full text-white font-semibold" style={{background:'var(--primary)'}}>Send message</button></form></main>}
