import {articles} from '@/lib/content';import {ArticleCard} from '@/components/article-card';import {notFound} from 'next/navigation';
export default async function Author({params}:{params:Promise<{slug:string}>}){const {slug}=await params;if(slug!=='nextbyte-editorial')return notFound();return <main className="container py-16"><div className="max-w-3xl"><div className="w-20 h-20 rounded-full grid place-items-center text-white text-xl font-black" style={{background:'var(--primary)'}}>NB</div><h1 className="text-5xl font-black mt-5">NextByte Editorial</h1><p className="muted text-lg mt-3 leading-8">A publication team focused on practical digital workflows, ecommerce, AI and web technology.</p><div className="mt-3 text-sm muted">Expertise: AI & Tech · Ecommerce · Productivity · Web & Tech</div></div><h2 className="text-3xl font-black mt-14">Articles by this author</h2><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6 mt-6">{articles.map(a=><ArticleCard key={a.slug} a={a}/>)}</div></main>}
