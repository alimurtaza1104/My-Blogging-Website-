import Link from 'next/link';import {ArrowRight,BookOpen,Layers,Search} from 'lucide-react';import {articles} from '@/lib/content';import {categories} from '@/lib/site';import {ArticleCard} from '@/components/article-card';
export default function Home(){const featured=articles[0];return <main><section className="container pt-20 pb-18"><div className="max-w-4xl"><div className="inline-flex items-center gap-2 text-sm font-semibold mb-6" style={{color:'var(--primary)'}}><span className="w-2 h-2 rounded-full" style={{background:'var(--teal)'}}/> Independent digital publication</div><h1 className="text-5xl md:text-7xl font-black tracking-[-.04em] leading-[.98]">Ideas that make the digital world easier to understand.</h1><p className="text-lg md:text-xl muted max-w-2xl mt-7 leading-8">NextByte Journal publishes practical, original guides about AI, ecommerce, productivity and web technology—written for people who want useful answers without unnecessary jargon.</p><div className="flex flex-wrap gap-3 mt-8"><Link href="/blog" className="px-5 py-3 rounded-full text-white font-semibold" style={{background:'var(--primary)'}}>Explore Latest Articles <ArrowRight className="inline ml-1" size={17}/></Link><Link href="/category/ai-tech" className="px-5 py-3 rounded-full border font-semibold" style={{borderColor:'var(--line)'}}>Browse Categories</Link></div></div></section><section className="container"><div className="grid lg:grid-cols-[1.5fr_1fr] gap-8 items-stretch"><Link href={`/blog/${featured.slug}`} className="relative overflow-hidden rounded-3xl min-h-[430px] group"><img src={featured.image} alt={featured.title} className="absolute inset-0 w-full h-full object-cover transition duration-700 group-hover:scale-105"/><div className="absolute inset-0 bg-gradient-to-t from-black/85 via-black/20 to-transparent"/><div className="absolute bottom-0 p-7 md:p-10 text-white max-w-2xl"><div className="text-sm font-semibold">Featured · {featured.category}</div><h2 className="text-3xl md:text-5xl font-black mt-2 leading-tight">{featured.title}</h2><p className="text-white/80 mt-3">{featured.excerpt}</p><div className="mt-5 text-sm">{featured.author} · {featured.date} · {featured.readingTime} min read</div></div></Link><div className="card p-7 flex flex-col justify-between"><div><BookOpen style={{color:'var(--primary)'}}/><h2 className="text-3xl font-bold mt-6">A calmer way to keep up with digital work.</h2><p className="muted mt-4 leading-7">Explore practical explainers and workflows designed to help you make better decisions about technology—not simply chase what is new.</p></div><Link href="/blog" className="font-semibold mt-8">Read the journal <ArrowRight className="inline ml-1" size={16}/></Link></div></div></section><section className="container py-20"><div className="flex items-end justify-between mb-8"><div><div className="text-sm font-bold uppercase tracking-wider" style={{color:'var(--primary)'}}>Latest</div><h2 className="text-4xl font-black mt-2">Fresh from the journal</h2></div><Link href="/blog" className="hidden sm:block font-semibold">View all</Link></div><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">{articles.slice(0,6).map(a=><ArticleCard key={a.slug} a={a}/>)}</div></section><section className="container pb-20"><div className="mb-8"><div className="text-sm font-bold uppercase tracking-wider" style={{color:'var(--teal)'}}>Explore</div><h2 className="text-4xl font-black mt-2">Choose your corner of the internet</h2></div><div className="grid md:grid-cols-2 gap-5">{categories.map(c=><Link key={c.slug} href={`/category/${c.slug}`} className="card p-7 hover:-translate-y-1 transition"><Layers style={{color:'var(--primary)'}}/><h3 className="text-2xl font-bold mt-5">{c.name}</h3><p className="muted mt-2 max-w-xl leading-7">{c.description}</p><span className="inline-block mt-5 font-semibold">Explore →</span></Link>)}</div></section><section className="container pb-20"><div className="card p-8 md:p-12 flex flex-col md:flex-row items-start md:items-center justify-between gap-8" style={{background:'linear-gradient(135deg,color-mix(in srgb,var(--primary) 9%,var(--surface)),var(--surface))'}}><div><div className="text-sm font-bold uppercase tracking-wider">Stay curious</div><h2 className="text-3xl font-black mt-2">Get practical ideas, not inbox noise.</h2><p className="muted mt-2">A simple newsletter for useful digital workflows and guides.</p></div><Link href="/contact" className="px-5 py-3 rounded-full text-white font-semibold" style={{background:'var(--text)',color:'var(--bg)'}}>Subscribe</Link></div></section></main>}
