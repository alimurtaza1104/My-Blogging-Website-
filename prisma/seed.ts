import {PrismaClient} from '@prisma/client';import {articles} from '../lib/content';import {categories} from '../lib/site';
const db=new PrismaClient();
async function main(){const author=await db.author.upsert({where:{slug:'nextbyte-editorial'},update:{},create:{name:'NextByte Editorial',slug:'nextbyte-editorial',bio:'A publication team focused on practical digital workflows, ecommerce, AI and web technology.',expertise:['AI & Tech','Ecommerce','Productivity','Web & Tech']}});for(const c of categories)await db.category.upsert({where:{slug:c.slug},update:{description:c.description},create:c});for(const a of articles){const c=await db.category.findUniqueOrThrow({where:{name:a.category}});await db.article.upsert({where:{slug:a.slug},update:{title:a.title,excerpt:a.excerpt,content:a.content,featuredImage:a.image,metaDescription:a.meta,readingTime:a.readingTime,publishedAt:new Date(a.date),status:'PUBLISHED',authorId:author.id,categoryId:c.id},create:{slug:a.slug,title:a.title,excerpt:a.excerpt,content:a.content,featuredImage:a.image,metaDescription:a.meta,readingTime:a.readingTime,publishedAt:new Date(a.date),status:'PUBLISHED',authorId:author.id,categoryId:c.id}})}}main().finally(()=>db.$disconnect());
