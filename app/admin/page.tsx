import {articles} from '@/lib/content';export default function Admin(){return <><div className="flex items-center justify-between"><div><div className="text-sm muted">Overview</div><h1 className="text-4xl font-black">Dashboard</h1></div><a href="/admin/articles/new" className="px-4 py-3 rounded-xl text-white font-semibold" style={{background:'var(--primary)'}}>New Article</a></div><div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-4 mt-8">{[['Total Articles',articles.length],['Published',articles.length],['Drafts',0],['Scheduled',0],['Comments',0],['Subscribers',0]].map(([x,n])=><div key={x as string} className="card p-6"><div className="muted text-sm">{x}</div><div className="text-3xl font-black mt-2">{n}</div></div>)}</div><div className="card mt-8 p-6"><h2 className="text-xl font-bold">Recent articles</h2><div className="mt-5 divide-y" style={{borderColor:'var(--line)'}}>{articles.slice(0,6).map(a=><div key={a.slug} className="py-4 flex justify-between gap-4"><span className="font-semibold">{a.title}</span><span className="muted text-sm">Published · {a.date}</span></div>)}</div></div></>}
