import {NextResponse} from 'next/server';import crypto from 'node:crypto';export async function POST(req:Request){const{email,password}=await req.json();const ok=crypto.timingSafeEqual(Buffer.from(String(email)),Buffer.from(String(process.env.ADMIN_EMAIL||'')))&&crypto.timingSafeEqual(Buffer.from(String(password)),Buffer.from(String(process.env.ADMIN_PASSWORD||'')));if(!ok)return NextResponse.json({error:'Unauthorized'},{status:401});const r=NextResponse.json({ok:true});r.cookies.set('nb_admin','authenticated',{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',maxAge:60*60*8});return r}
