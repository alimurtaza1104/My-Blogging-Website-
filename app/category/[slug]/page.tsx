import {categories} from '@/lib/site';import {articles} from '@/lib/content';import {ArticleCard} from '@/components/article-card';import {notFound} from 'next/navigation';
export default async function Category({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const c=categories.find(x=>x.slug===slug);if(!c)return notFound();const list=articles.filter(a=>a.category===c.name);return <main className="container py-16"><div className="max-w-3xl"><div className="text-sm font-bold uppercase tracking-wider" style={{color:'var(--teal)'}}>Category</div><h1 className="text-5xl font-black mt-2">{c.name}</h1><p className="muted text-lg mt-4 leading-8">{c.description}</p></div><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6 mt-10">{list.map(a=><ArticleCard key={a.slug} a={a}/>)}</div></main>}
