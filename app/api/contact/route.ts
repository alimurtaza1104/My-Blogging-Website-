import {NextResponse} from 'next/server';import {db} from '@/lib/db';export async function POST(req:Request){try{const b=await req.json();if(b.website)return NextResponse.json({ok:true});if(!b.name||!b.email||!b.subject||!b.message)return NextResponse.json({error:'Invalid request'},{status:400});await db.contactMessage.create({data:{name:String(b.name).slice(0,120),email:String(b.email).slice(0,200),subject:String(b.subject).slice(0,200),message:String(b.message).slice(0,5000)}});return NextResponse.json({ok:true})}catch{return NextResponse.json({error:'Server error'},{status:500})}}
