import Link from 'next/link';import {Search} from 'lucide-react';export default function NotFound(){return <main className="container py-28 text-center"><div className="text-7xl font-black" style={{color:'var(--primary)'}}>404</div><h1 className="text-4xl font-black mt-4">Looks like this page took a wrong turn.</h1><p className="muted mt-3">The page may have moved or never existed.</p><div className="flex justify-center gap-3 mt-7"><Link className="px-5 py-3 rounded-full text-white" style={{background:'var(--primary)'}} href="/">Back Home</Link><Link className="px-5 py-3 rounded-full border" style={{borderColor:'var(--line)'}} href="/blog">Browse Articles</Link><Link className="px-5 py-3 rounded-full border" style={{borderColor:'var(--line)'}} href="/search"><Search className="inline" size={17}/> Search</Link></div></main>}
