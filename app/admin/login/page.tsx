'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const router=useRouter();async function go(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});if(r.ok)router.push('/admin');else setError('Invalid administrator credentials.')}return <main className="min-h-screen grid place-items-center p-6"><form onSubmit={go} className="card p-8 w-full max-w-md"><div className="w-11 h-11 rounded-xl grid place-items-center text-white font-bold" style={{background:'var(--primary)'}}>N</div><h1 className="text-3xl font-black mt-5">Admin login</h1><p className="muted mt-2">Use the credentials configured in your environment.</p>{error&&<p className="text-sm mt-4" style={{color:'#c0392b'}}>{error}</p>}<div className="grid gap-4 mt-6"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="border rounded-xl px-4 py-3 bg-transparent" style={{borderColor:'var(--line)'}}/><input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="border rounded-xl px-4 py-3 bg-transparent" style={{borderColor:'var(--line)'}}/><button className="py-3 rounded-xl text-white font-semibold" style={{background:'var(--primary)'}}>Sign in</button></div></form></main>}
