import type {MetadataRoute} from 'next';import {articles} from '@/lib/content';import {categories} from '@/lib/site';const base=process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000';export default function sitemap():MetadataRoute.Sitemap{const pages=['','/blog','/about','/contact','/authors','/policies/privacy','/policies/terms','/policies/disclaimer','/policies/editorial','/policies/cookies'];return [...pages.map(path=>({url:base+path,lastModified:new Date()})),...categories.map(c=>({url:`${base}/category/${c.slug}`,lastModified:new Date()})),...articles.map(a=>({url:`${base}/blog/${a.slug}`,lastModified:new Date(a.updated)})),{url:`${base}/author/nextbyte-editorial`,lastModified:new Date()}]}
